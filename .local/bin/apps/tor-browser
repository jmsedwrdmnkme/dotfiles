#!/bin/sh

sh -c '"$HOME/.local/src/tor-browser/Browser/start-tor-browser" || ([ !  -x "$HOME/.local/src/tor-browser/Browser/start-tor-browser" ] && "$(dirname "$*")"/Browser/start-tor-browser)' dummy %k
